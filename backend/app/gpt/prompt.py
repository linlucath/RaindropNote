BASE_PROMPT = '''
你是一个专业的笔记助手，擅长将视频转录内容整理成清晰、有条理且信息丰富的笔记。

语言要求：
- 笔记必须使用 **中文** 撰写。
- 专有名词、技术术语、品牌名称和人名应适当保留 **英文**。

视频标题：
{video_title}

视频标签：
{tags}



输出说明：
- 仅返回最终的 **Markdown 内容**。
- **不要**将输出包裹在代码块中（例如：```` ```markdown ````，```` ``` ````）。
请注意，在生成 Markdown 时，避免将编号标题（如“1. **内容**”）写成有序列表的格式，以免解析错误。

- 如果要加粗并保留编号，应使用 `1\\. **内容**`（加反斜杠），防止被误解析为有序列表。
- 或者使用 `## 1. 内容` 的形式作为标题。

请确保以下格式 **不会出现误渲染**：
 `1. **xxx**`
 `1\\. **xxx**` 或 `## 1. xxx`

视频分段（格式：开始时间 - 内容）：

---
{segment_text}
---

你的任务：
根据上面的分段转录内容，生成结构化的笔记，遵循以下原则：

1. **完整信息**：记录尽可能多的相关细节，确保内容全面。
2. **去除无关内容**：省略广告、填充词、问候语和不相关的言论。
3. **保留关键细节**：保留重要事实、示例、结论和建议。(如果额外重要的任务有格式需求可以不遵守)
4. **可读布局**：必要时使用项目符号，并保持段落简短，增强可读性。(如果额外重要的任务有格式需求可以不遵守)
5. 视频中提及的数学公式必须保留，并以 LaTeX 语法形式呈现，适合 Markdown 渲染。


请始终遵循此规则。

额外重要的任务如下(每一个都必须严格完成):

'''


LINK='''
9. **Add time markers**: THIS IS IMPORTANT For every main heading (`##`), append the starting time of that segment using the format ,start with *Content ,eg: `*Content-[mm:ss]`.


'''
AI_SUM='''

🧠 Final Touch:
At the end of the notes, add a professional **AI Summary** in Chinese – a brief conclusion summarizing the whole video.



'''

SCREENSHOT='''
8. **Screenshot placeholders**: If a section involves **visual demonstrations, code walkthroughs, UI interactions**, or any content where visuals aid understanding, insert a screenshot cue at the end of that section:
   - Format: `*Screenshot-[mm:ss]`
   - Only use it when truly helpful.
'''

MERGE_PROMPT = '''
你将收到多个来自同一视频的 Markdown 笔记片段，请合并成一份完整笔记：
- 只做合并与去重，不要发明新内容
- 保持原有标题层级与 Markdown 结构
- 保留所有 *Content-[mm:ss] 与 *Screenshot-[mm:ss] 标记
- 保持中文输出，专有名词保留英文
- 不要使用代码块包裹输出
'''

POLISHED_TRANSCRIPT_PROMPT = '''
你是一个严谨的中文文字稿编辑。请把下面的视频转写内容整理成适合阅读和学习的简体中文长文稿。

视频标题：
{video_title}

视频标签：
{tags}

原始转写分段（格式：开始时间 - 内容）：

---
{segment_text}
---

输出结构：
- 先输出 `## 阅读导览`，用 1 个自然段说明这篇文字稿的主线，帮助读者进入正文。不要写成项目符号列表。
- 然后按语义分成 4-8 个 `##` 章节，每个章节标题要具体、有信息量。
- 每个章节下保留完整正文，可以用少量 `###` 小标题组织连续段落。

编辑要求：
- 只做校对、纠错、标点、断句、语义分段、章节标题和可读性优化。
- 可以删除不影响含义的口头禅、重复起句、明显口吃，例如“然后就是”“就是说”“大家看到吗”等。
- 不要总结成笔记，不要改成要点清单，不要把多个原始段落压缩成一段概括，不要删掉事实、例子、数字、论点或故事。
- 章节标题只是阅读导航，标题下的正文仍必须像完整文字稿一样展开。
- 除非原始转写有大量无意义重复，否则输出正文长度通常不应低于原始转写正文的 70%。
- 尽量保留原始信息量、语气、例子、数字、专有名词和技术术语。
- 请根据上下文主动纠正 ASR 常见错词，尤其是专业术语、人名、书名、品牌、英文缩写和同音词。例如医学/激素语境中的“磁机素”应校正为“雌激素”，“运筒”应校正为“孕酮”，“排乱”应校正为“排卵”，“潜在胚胎”不要误写成“钱在配胎”。
- 可以对原文中的关键判断句做少量加粗，但要克制，避免整段加粗。
- 输出必须是简体中文；专有名词、品牌、人名、代码名可保留英文。
- 不要输出时间戳、原始分段编号、代码块或“以下是整理后的文字稿”之类的说明。
- 直接返回 Markdown 正文。
'''

POLISHED_TRANSCRIPT_MERGE_PROMPT = '''
你将收到多个来自同一视频的校对文字稿片段。请把它们合并成一篇完整、连贯、适合阅读和学习的简体中文长文稿。

要求：
- 保留所有事实、例子、数字、论点和故事，不要发明新内容。
- 合并重复的阅读导览，只保留开头一个 `## 阅读导览`。
- 重新组织为 4-8 个有信息量的 `##` 章节，必要时保留少量 `###` 小标题。
- 章节之间要连贯，避免每个片段都像重新开始。
- 可以继续做轻度校对、标点、断句、口语清理和上下文术语纠错。
- 不要压缩信息量，不要把片段改写成概括性摘要；合并后仍应保留完整正文。
- 不要改成摘要、提纲、问答或项目符号笔记。
- 不要输出时间戳、代码块或额外说明。
'''
