# 雨滴笔记助手前端

React 19 + Vite + TypeScript 前端，用于雨滴笔记助手的内容导入、任务进度、文字稿预览、设置和历史管理。

## 开发

```bash
pnpm install
pnpm dev
```

开发服务默认运行在 `http://localhost:3015`，接口通过 Vite 代理访问后端。

## 构建

```bash
pnpm build
pnpm lint
```
